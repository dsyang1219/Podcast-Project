# Regenerable HTTP cache (rebuilt from the frozen chart + live feeds)
data/cache/

# Generated outputs (rebuilt by `python -m pipeline.run`). The small
# deliverables are committed for convenience; the large, fully-regenerable
# per-episode file is not.
# NOTE: the parent repo's root .gitignore has a broad `output/` rule for an
# unrelated project; re-include this project's data/output/ explicitly.
!/data/output/
data/output/episodes.csv.gz

# External Brookings export — third-party data, drop in locally.
# The frozen Apple chart under data/raw/ IS committed (reproducible frame).
data/external/*
!data/external/.gitkeep

# Downloaded audio (hundreds of GB) and transcribed text — regenerable from
# data/output/sample_out/ + transcription/, not suitable for git.
data/audio_queue/
data/transcripts/

# Local Python environment
.venv/

# API keys / secrets
.env

# Fetch/download logs
transcription/download_audio.log

__pycache__/
*.pyc

# Large regenerable model artifacts (added 2026-08-05). data/output/ is
# re-included above, so without these every derived matrix shows up as
# untracked and a blanket `git add` would try to push ~14GB -- past GitHub's
# 100MB-per-file hard limit. All of these are rebuilt by their own scripts;
# the small result JSON/CSV and the figures alongside them ARE committed.
*.npy
*.rds
*.pkl
data/output/chunks_*.csv
data/output/sentences_*.csv
data/output/lda_doctopic_*.csv
data/output/adspan/chunks_*.csv
data/output/adspan/doctopic_*.csv
data/output/adspan/*.jsonl.gz
data/output/guests_raw*.jsonl
__pycache__/
*.pyc
r/lib/
.venv-tlda/
